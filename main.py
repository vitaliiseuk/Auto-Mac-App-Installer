import commandsimport timeimport getpassimport osdef main():	cmd = 'open wine.dmg'	a = commands.getstatusoutput(cmd)		print 'Wine package opened.'	time.sleep(8)		cmd = 'cp -a /Volumes/WineBottler\ Combo/Wine.app /Applications'	print 'on installing...'	a = commands.getstatusoutput(cmd)		print 'Finished.'	cmd = 'diskutil unmount /Volumes/WineBottler\ Combo/'	a = commands.getstatusoutput(cmd)		cmd = 'open sample.exe'	a = commands.getstatusoutput(cmd)		paths = dict()	with open('urls.txt') as fp:		for item in fp:			temp = item.split('=')			paths[temp[0]] = temp[1].strip('\n')			user_name = getpass.getuser()	url_check = '/Users/' + user_name + paths['RUN_PATH']	temp1 = paths['RUN_PATH'].replace(' ', '\ ')	url = '/Users/' + user_name + temp1			while True:		time.sleep(8)			if os.path.exists(url_check):			cmd = 'open ' + url			a = commands.getstatusoutput(cmd)			break		cmd = 'cp -a ../../../run.app ~/Desktop'	a = commands.getstatusoutput(cmd)	pass	if __name__ == '__main__':	main()